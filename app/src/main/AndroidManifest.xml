<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="true"
            android:debuggable="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="HardcodedDebugMode">
        <service
                android:name=".ReceiveService"
                android:enabled="true"
                android:exported="true">
        </service>

        <activity android:name=".startPageTest">
        </activity>
        <activity
                android:name=".IFcheckView"
                android:label="@string/title_activity_ifcheck_view"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".SignUpView">
        </activity>
        <activity android:name=".SelectPicture">
        </activity>
        <activity android:name=".tutorialPageMock">
        </activity>
        <activity android:name=".Confirmation">
        </activity>
        <activity android:name=".ServeView">
        </activity>
        <activity android:name=".ReceiveView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ReceiveDetailView">
        </activity>
    </application>

</manifest>